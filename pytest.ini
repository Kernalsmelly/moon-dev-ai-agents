[pytest]
# Only collect files prefixed with test_ to avoid collecting script files like *_test.py
python_files = test_*.py

# Don't recurse into large data folders
norecursedirs = data src/data src/data/rbi_v3 src/data/rbi
